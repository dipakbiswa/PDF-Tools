export const metadata = {
  title: "Password Protect PDF Free Online",
  description: "Encrypt your PDF with a password to prevent unauthorised access. Free, private, AES-128.",
};

export default function Layout({ children }) {
  return children;
}
