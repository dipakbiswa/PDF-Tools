export const metadata = {
  title: "PDF to Images Free Online",
  description: "Convert every PDF page to PNG or JPEG images. Free, private, bulk ZIP download supported.",
};

export default function Layout({ children }) {
  return children;
}
