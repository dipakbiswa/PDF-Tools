export const metadata = {
  title: "Remove PDF Pages Free Online",
  description: "Select and delete specific pages from your PDF document. Free, private, instant — no upload needed.",
};

export default function Layout({ children }) {
  return children;
}
