export const metadata = {
  title: "Split PDF Free Online",
  description: "Extract pages or split a PDF into multiple files online, free. Works entirely in your browser.",
};

export default function Layout({ children }) {
  return children;
}
