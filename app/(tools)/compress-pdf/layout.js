export const metadata = {
  title: "Compress PDF Free Online",
  description: "Reduce your PDF file size for free. Works in the browser — your files never leave your device.",
};

export default function Layout({ children }) {
  return children;
}
