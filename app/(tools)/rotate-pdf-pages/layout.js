export const metadata = {
  title: "Rotate PDF Pages Free Online",
  description: "Rotate individual pages or the entire PDF. Free, private, instant — no upload needed.",
};

export default function Layout({ children }) {
  return children;
}
