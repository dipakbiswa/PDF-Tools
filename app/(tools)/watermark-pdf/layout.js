export const metadata = {
  title: "Add Watermark to PDF Free Online",
  description: "Stamp a custom text watermark on every page of your PDF. Free, private, no upload needed.",
};

export default function Layout({ children }) {
  return children;
}
